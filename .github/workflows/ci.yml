name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI placeholder
        run: |
          echo "CI skeleton in place. Add lint/test/build steps once codebase is initialized."
